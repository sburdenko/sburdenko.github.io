/** Tape 05 strings: Factory, Object Pool, Singleton. */
import { facts } from './facts.js?v=202609162304';

export const CREATION = {
  'factory.h2': { en: 'Factory: ask for a product, not for a type', ru: 'Factory: проси продукт, а не тип' },
  'factory.prose': {
    en: '<p>Games spawn many kinds of things, and often you only know which one at runtime. A <b>factory</b> is an object whose job is to create other objects, so the caller does not repeat <code>Instantiate</code> + setup for every concrete type.</p><p>The book’s version: products implement <code>IProduct</code> (a name and <code>Initialize()</code>), factories derive from an abstract <code>Factory</code> with <code>GetProduct(Vector3 position)</code>. <code>ConcreteFactoryA</code> instantiates its prefab and calls <code>Initialize()</code>; what that does — particles for A, a sound for B — lives inside the product. The factory itself stays short.</p><p>Improvements from the book: keep products in a dictionary keyed by id, make the factory manager static, use it for plain C# objects too, and let a factory hand out pooled objects instead of instantiating new ones.</p>',
    ru: '<p>Игра создаёт много разных объектов, и часто только во время игры понятно, какой именно нужен. <b>Фабрика</b> — объект, чья работа создавать другие объекты, чтобы вызывающий код не повторял <code>Instantiate</code> и настройку для каждого конкретного типа.</p><p>Вариант из книги: продукты реализуют <code>IProduct</code> (имя и <code>Initialize()</code>), фабрики наследуют абстрактную <code>Factory</code> с методом <code>GetProduct(Vector3 position)</code>. <code>ConcreteFactoryA</code> создаёт свой префаб и вызывает <code>Initialize()</code>; что именно происходит — частицы у A, звук у B — живёт внутри продукта. Сама фабрика остаётся короткой.</p><p>Улучшения из книги: хранить продукты в словаре по id, сделать менеджер фабрик статическим, применять к обычным C#-объектам и отдавать объекты из пула вместо нового <code>Instantiate</code>.</p>',
  },
  'factory.facts': {
    en: facts('en', 'New product types without touching the spawning client; initialization stays next to the product.', 'An interface, a base factory and one factory per product — overhead when there are only two kinds.', 'Many product variants, spawned from several places, each with its own setup.'),
    ru: facts('ru', 'Новые типы продуктов без правки клиента; инициализация лежит рядом с продуктом.', 'Интерфейс, базовая фабрика и по фабрике на продукт — лишнее, если видов всего два.', 'Много вариантов продукта, создаются из разных мест, у каждого своя настройка.'),
  },
  'factory.labTag': { en: 'INTERACTIVE · CLICK TO CREATE', ru: 'ИНТЕРАКТИВ · CLICK TO CREATE' },
  'factory.labH': { en: 'Click the field: the chosen factory builds a product there', ru: 'Щёлкни по полю: выбранная фабрика создаст там продукт' },
  'factory.segAria': { en: 'Active factory', ru: 'Активная фабрика' },
  'factory.addC': { en: '+ Add ProductC', ru: '+ Добавить ProductC' },
  'factory.clear': { en: 'Clear', ru: 'Очистить' },
  'factory.canvasAria': { en: 'Field where factories place products', ru: 'Поле, где фабрики создают продукты' },
  'factory.click': { en: 'CLICK TO CREATE', ru: 'ЩЁЛКНИ, ЧТОБЫ СОЗДАТЬ' },
  'factory.empty': { en: 'click anywhere', ru: 'щёлкни где угодно' },
  'factory.costSwitch': {
    en: c => `<b>switch-based Spawner</b><span>${c.caseBranches} case branches in one method</span><span>edits to the client: ${c.editsToClient}</span>`,
    ru: c => `<b>Spawner со switch</b><span>${c.caseBranches} веток case в одном методе</span><span>правок клиента: ${c.editsToClient}</span>`,
  },
  'factory.costFactory': {
    en: c => `<b>Factory pattern</b><span>new classes: ${c.newClasses}</span><span>edits to the client: ${c.editsToClient}</span>`,
    ru: c => `<b>Паттерн Factory</b><span>новых классов: ${c.newClasses}</span><span>правок клиента: ${c.editsToClient}</span>`,
  },

  'pool.h2': { en: 'Object Pool: stop feeding the garbage collector', ru: 'Object Pool: перестань кормить сборщик мусора' },
  'pool.prose': {
    en: '<p>Creating and destroying many short-lived GameObjects — bullets, hit effects, damage numbers — costs <code>Instantiate</code> time and leaves garbage for the GC, which shows up as spikes. A <b>pool</b> keeps deactivated instances and hands them out again: <i>get</i> enables one, <i>release</i> disables it and puts it back. <code>ParticleSystem</code> already works this way with its max particle count.</p><p>The manual version in the book stores instances in a <code>Stack&lt;PooledObject&gt;</code>, pre-fills it in <code>SetupPool()</code> and creates a new instance only when the stack is empty. Since Unity 2021 you rarely write it yourself: <code>UnityEngine.Pool.ObjectPool&lt;T&gt;</code> takes four callbacks — create, on get, on release, on destroy — plus <code>collectionCheck</code>, <code>defaultCapacity</code> and <code>maxSize</code>.</p><p>Fill pools where a stall is invisible, for example behind a loading screen. There are also <code>ListPool</code>, <code>HashSetPool</code> and <code>DictionaryPool</code> for temporary collections.</p>',
    ru: '<p>Создание и удаление множества короткоживущих GameObject — пуль, эффектов попадания, цифр урона — стоит времени <code>Instantiate</code> и оставляет мусор для GC, который виден как всплески. <b>Пул</b> хранит выключенные экземпляры и выдаёт их снова: <i>get</i> включает объект, <i>release</i> выключает и кладёт обратно. <code>ParticleSystem</code> уже так работает со своим лимитом частиц.</p><p>Ручная версия в книге хранит экземпляры в <code>Stack&lt;PooledObject&gt;</code>, заполняет его в <code>SetupPool()</code> и создаёт новый объект только при пустом стеке. С Unity 2021 это редко пишут сами: <code>UnityEngine.Pool.ObjectPool&lt;T&gt;</code> принимает четыре колбэка — создать, при выдаче, при возврате, при уничтожении — и параметры <code>collectionCheck</code>, <code>defaultCapacity</code> и <code>maxSize</code>.</p><p>Заполняй пулы там, где задержку не видно, например за экраном загрузки. Для временных коллекций есть <code>ListPool</code>, <code>HashSetPool</code> и <code>DictionaryPool</code>.</p>',
  },
  'pool.facts': {
    en: facts('en', 'No per-shot allocation and no garbage after warm-up; the create cost moves to load time.', 'Memory held by idle objects, state that must be reset on get, bugs from releasing twice or using after release.', 'The Profiler shows GC.Alloc or Instantiate from frequently spawned, short-lived objects.'),
    ru: facts('ru', 'Нет аллокации на выстрел и мусора после прогрева; цена создания переезжает в загрузку.', 'Память под простаивающие объекты, состояние нужно сбрасывать при выдаче, баги от двойного release и использования после возврата.', 'Profiler показывает GC.Alloc или Instantiate от часто создаваемых короткоживущих объектов.'),
  },
  'pool.labTag': { en: 'INTERACTIVE · GUN AND POOL', ru: 'ИНТЕРАКТИВ · ОРУЖИЕ И ПУЛ' },
  'pool.labH': { en: 'Fire, then stop firing, and watch what maxSize really limits', ru: 'Стреляй, потом прекрати — и посмотри, что на самом деле ограничивает maxSize' },
  'pool.mode': { en: 'SPAWNING', ru: 'СОЗДАНИЕ' },
  'pool.rate': { en: 'Shots per second', ru: 'Выстрелов в секунду' },
  'pool.life': { en: 'Bullet lifetime', ru: 'Время жизни пули' },
  'pool.prewarm': { en: 'Prewarm on load', ru: 'Прогрев при загрузке' },
  'pool.hold': { en: '❚❚ Release trigger', ru: '❚❚ Отпустить курок' },
  'pool.release': { en: '► Hold trigger', ru: '► Зажать курок' },
  'pool.burst': { en: 'Burst', ru: 'Очередь' },
  'pool.reset': { en: 'Reset', ru: 'Сброс' },
  'pool.double': { en: 'Release the same bullet twice', ru: 'Вернуть одну пулю дважды' },
  'pool.canvasAria': { en: 'Bullets in flight, the pool stack and a timeline of creations and destructions', ru: 'Пули в полёте, стек пула и лента созданий и уничтожений' },
  'pool.active': { en: 'Active', ru: 'Активных' },
  'pool.inactive': { en: 'In pool', ru: 'В пуле' },
  'pool.created': { en: 'Created', ru: 'Создано' },
  'pool.destroyed': { en: 'Destroyed', ru: 'Уничтожено' },
  'pool.timeline': { en: 'LAST 8 SECONDS', ru: 'ПОСЛЕДНИЕ 8 СЕКУНД' },
  'pool.lineCreate': { en: '▲ create / Instantiate', ru: '▲ создание / Instantiate' },
  'pool.lineDestroy': { en: '▼ destroy → garbage', ru: '▼ уничтожение → мусор' },
  'pool.garbage': { en: n => `GARBAGE ${n}`, ru: n => `МУСОР ${n}` },
  'pool.noGarbage': { en: 'NO GARBAGE', ru: 'МУСОРА НЕТ' },
  'pool.doubleThrow': {
    en: '<b>InvalidOperationException:</b> “Trying to release an object that has already been released to the pool.” <code>collectionCheck</code> catches it — the check is Editor-friendly but costs a search on every release.',
    ru: '<b>InvalidOperationException:</b> «Trying to release an object that has already been released to the pool». <code>collectionCheck</code> ловит ошибку — проверка удобна в разработке, но стоит поиска при каждом release.',
  },
  'pool.doubleCorrupt': {
    en: '<b>Silent corruption:</b> the same object now sits in the stack twice. Two future <code>Get()</code> calls return one instance to two different shooters.',
    ru: '<b>Тихая порча:</b> один объект теперь лежит в стеке дважды. Два будущих <code>Get()</code> отдадут один экземпляр двум разным стрелкам.',
  },
  'pool.errata': {
    en: '<b>Checked against the book and the API</b><p><code>defaultCapacity</code> is not a prewarm: it only sizes the internal stack, nothing is created until the first <code>Get()</code>. To prewarm, call <code>Get()</code> N times and then <code>Release()</code> them during loading. <code>maxSize</code> does not cap how many objects can be active — <code>Get()</code> on an empty pool always creates. It caps how many <i>inactive</i> objects are kept: releases beyond it go to <code>actionOnDestroy</code>. A steady stream never hits it; a burst followed by silence does. In the book’s manual pool, the object created when the stack is empty is returned without <code>SetActive(true)</code>, so it only works if the prefab is saved active.</p>',
    ru: '<b>Сверено с книгой и API</b><p><code>defaultCapacity</code> — не прогрев: он лишь задаёт размер внутреннего стека, ничего не создаётся до первого <code>Get()</code>. Чтобы прогреть, вызови <code>Get()</code> N раз и затем <code>Release()</code> во время загрузки. <code>maxSize</code> не ограничивает число активных объектов — <code>Get()</code> на пустом пуле всегда создаёт. Он ограничивает число <i>неактивных</i> хранимых объектов: лишние возвраты уходят в <code>actionOnDestroy</code>. Ровный поток его не задевает, а очередь с последующей тишиной — задевает. В ручном пуле из книги объект, созданный при пустом стеке, возвращается без <code>SetActive(true)</code> — это работает, только если префаб сохранён активным.</p>',
  },

  'singleton.h2': { en: 'Singleton: one instance, global access — and the bill', ru: 'Singleton: один экземпляр, глобальный доступ — и счёт за это' },
  'singleton.prose': {
    en: '<p>The Gang of Four definition: a class allows only one instance of itself and gives global access to it. In Unity that usually means a manager — game flow, audio, save system.</p><p>The <b>simple</b> version stores <code>this</code> in a static <code>Instance</code> in <code>Awake</code> and destroys any duplicate. It has two problems: the object dies with its scene, and it must already be placed in the scene. The <b>improved</b> version calls <code>DontDestroyOnLoad</code> to survive scene loads and creates itself lazily on first access. The generic <code>Singleton&lt;T&gt;</code> removes the copy-paste for several managers.</p><p>The book is honest: singletons hide dependencies, make tests interfere with each other and couple everything to one concrete class. Game Programming Patterns calls it an anti-pattern. For a small game it is quick, simple and fast — keep the number of singletons to a handful.</p>',
    ru: '<p>Определение «банды четырёх»: класс допускает только один свой экземпляр и даёт к нему глобальный доступ. В Unity это обычно менеджер — игрового цикла, звука, сохранений.</p><p><b>Простая</b> версия в <code>Awake</code> кладёт <code>this</code> в статический <code>Instance</code> и уничтожает дубликаты. Проблем две: объект погибает вместе со сценой и должен заранее стоять в сцене. <b>Улучшенная</b> версия вызывает <code>DontDestroyOnLoad</code>, чтобы пережить загрузку, и создаёт себя лениво при первом обращении. Обобщённый <code>Singleton&lt;T&gt;</code> убирает копипасту для нескольких менеджеров.</p><p>Книга честна: синглтоны прячут зависимости, заставляют тесты мешать друг другу и связывают всё с одним конкретным классом. Game Programming Patterns называет его антипаттерном. Для небольшой игры он быстрый и простой — держи число синглтонов в пределах нескольких штук.</p>',
  },
  'singleton.facts': {
    en: facts('en', 'Access from anywhere without GetComponent/Find; exactly one manager.', 'Hidden dependencies, global mutable state, harder unit tests, lifetime and scene-order bugs.', 'A small game, a truly global service, and you accept the coupling. Otherwise pass references explicitly.'),
    ru: facts('ru', 'Доступ отовсюду без GetComponent/Find; ровно один менеджер.', 'Скрытые зависимости, глобальное изменяемое состояние, сложные юнит-тесты, баги времени жизни и порядка сцен.', 'Небольшая игра, действительно глобальный сервис, и связанность приемлема. Иначе передавай ссылки явно.'),
  },
  'singleton.labTag': { en: 'INTERACTIVE · SCENE LOADS', ru: 'ИНТЕРАКТИВ · ЗАГРУЗКА СЦЕН' },
  'singleton.labH': { en: 'Score points, load another scene, and watch where the state goes', ru: 'Набери очки, загрузи другую сцену и посмотри, куда девается состояние' },
  'singleton.inB': { en: 'GameManager is also placed in scene B', ru: 'GameManager стоит и в сцене B' },
  'singleton.score': { en: 'GameManager.Instance.AddScore()', ru: 'GameManager.Instance.AddScore()' },
  'singleton.loadA': { en: 'Load scene A', ru: 'Загрузить сцену A' },
  'singleton.loadB': { en: 'Load scene B', ru: 'Загрузить сцену B' },
  'singleton.reset': { en: 'Reset', ru: 'Сброс' },
  'singleton.noManager': { en: 'no GameManager', ru: 'нет GameManager' },
  'singleton.destroyed': { en: 'destroyed', ru: 'уничтожен' },
  'singleton.scoreLabel': { en: 'Score', ru: 'Очки' },
  'singleton.sceneLabel': { en: 'Active scene', ru: 'Активная сцена' },
  'singleton.log.assign': { en: id => `Awake #${id}: Instance == null → Instance = this`, ru: id => `Awake #${id}: Instance == null → Instance = this` },
  'singleton.log.persist': { en: id => `Awake #${id}: Instance = this, DontDestroyOnLoad(gameObject)`, ru: id => `Awake #${id}: Instance = this, DontDestroyOnLoad(gameObject)` },
  'singleton.log.duplicate': { en: id => `Awake #${id}: an instance already exists → Destroy(gameObject)`, ru: id => `Awake #${id}: экземпляр уже есть → Destroy(gameObject)` },
  'singleton.log.load': { en: scene => `SceneManager.LoadScene("${scene}") — objects of the old scene are destroyed`, ru: scene => `SceneManager.LoadScene("${scene}") — объекты старой сцены уничтожены` },
  'singleton.log.score': { en: n => `AddScore() → score ${n}`, ru: n => `AddScore() → очки ${n}` },
  'singleton.log.lazy': { en: id => `Instance was null → SetupInstance() created GameManager #${id} under DontDestroyOnLoad`, ru: id => `Instance был null → SetupInstance() создал GameManager #${id} в DontDestroyOnLoad` },
  'singleton.log.null': { en: () => 'NullReferenceException: Instance is null', ru: () => 'NullReferenceException: Instance равен null' },
  'singleton.log.missing': {
    en: () => 'Instance points at a destroyed object: Unity says Instance == null, but C# still holds the reference. Pure C# code writes into a zombie; the first gameObject/transform access throws MissingReferenceException.',
    ru: () => 'Instance указывает на уничтоженный объект: Unity говорит Instance == null, но C# всё ещё держит ссылку. Чистый C#-код пишет в «зомби», а первое обращение к gameObject/transform бросит MissingReferenceException.',
  },
  'singleton.errata': {
    en: '<b>Checked against the book</b><p><code>FindObjectOfType</code> from the book is obsolete in Unity 6 — use <code>FindAnyObjectByType</code> (fast, any match) or <code>FindFirstObjectByType</code>. With “Enter Play Mode Options” and domain reload disabled, the static field survives between play sessions and points at a destroyed object: reset it in a <code>[RuntimeInitializeOnLoadMethod(RuntimeInitializeLoadType.SubsystemRegistration)]</code> method. A lazily created singleton accessed from another object’s <code>OnDestroy</code> while the game quits spawns a new object and triggers “Some objects were not cleaned up”. Derived classes that override the virtual <code>Awake</code> must call <code>base.Awake()</code>.</p>',
    ru: '<b>Сверено с книгой</b><p><code>FindObjectOfType</code> из книги устарел в Unity 6 — используй <code>FindAnyObjectByType</code> (быстрый, любой найденный) или <code>FindFirstObjectByType</code>. С «Enter Play Mode Options» и выключенной перезагрузкой домена статическое поле переживает запуск и указывает на уничтоженный объект: сбрасывай его в методе с <code>[RuntimeInitializeOnLoadMethod(RuntimeInitializeLoadType.SubsystemRegistration)]</code>. Ленивый синглтон, к которому обращаются из <code>OnDestroy</code> другого объекта при выходе из игры, создаёт новый объект и вызывает «Some objects were not cleaned up». Наследник, переопределивший виртуальный <code>Awake</code>, обязан вызвать <code>base.Awake()</code>.</p>',
  },
};
