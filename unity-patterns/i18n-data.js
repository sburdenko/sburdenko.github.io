/** Tape 05 strings: MVP / MVVM, Flyweight, Dirty Flag. */
import { facts } from './facts.js?v=202609162304';

export const DATA = {
  'ui.h2': { en: 'MVP and MVVM: keep data, logic and UI apart', ru: 'MVP и MVVM: данные, логика и UI отдельно' },
  'ui.prose': {
    en: '<p><b>MVC</b> splits an application into a Model that stores data, a View that shows it and a Controller that computes changes. In Unity the UI framework already is the View, so most teams use <b>MVP</b>: the View captures input and forwards it to a <i>Presenter</i>, the Presenter updates the Model, the Model raises an event, and the Presenter refreshes the View. The View never reads the Model directly.</p><p>The book’s health bar: <code>HealthModel</code> is a ScriptableObject with <code>CurrentHealth</code>, <code>Increment/Decrement/Restore</code> and a <code>HealthChanged</code> event. <code>HealthPresenter</code> subscribes and in <code>UpdateUI()</code> converts the number into bar value, bar colour, a “Good / Neutral / Danger” label and the value text.</p><p>Much of that presenter is shuttling data. Unity 6 UI Toolkit adds <b>runtime data binding</b>, which turns MVP into <b>MVVM</b>: the View Model sets up bindings from model properties to UI properties, and <code>ConverterGroup</code>s turn an int into a colour or a status string. Bindings can be authored in UI Builder (Add binding… → data source, path, mode) or in C# with <code>DataBinding</code> and <code>SetBinding</code> — needed for sub-elements like a ProgressBar’s fill.</p>',
    ru: '<p><b>MVC</b> делит приложение на Model, которая хранит данные, View, которое их показывает, и Controller, который вычисляет изменения. В Unity фреймворк UI уже и есть View, поэтому чаще используют <b>MVP</b>: View ловит ввод и передаёт его <i>Presenter</i>, тот меняет Model, Model поднимает событие, и Presenter обновляет View. View никогда не читает Model напрямую.</p><p>Полоска здоровья из книги: <code>HealthModel</code> — ScriptableObject с <code>CurrentHealth</code>, <code>Increment/Decrement/Restore</code> и событием <code>HealthChanged</code>. <code>HealthPresenter</code> подписывается и в <code>UpdateUI()</code> превращает число в значение полосы, её цвет, надпись «Good / Neutral / Danger» и текст значения.</p><p>Большая часть презентера просто перекладывает данные. UI Toolkit в Unity 6 добавляет <b>runtime data binding</b>, и MVP превращается в <b>MVVM</b>: View Model задаёт привязки свойств модели к свойствам UI, а <code>ConverterGroup</code> превращает int в цвет или строку статуса. Привязки настраиваются в UI Builder (Add binding… → источник, путь, режим) или в C# через <code>DataBinding</code> и <code>SetBinding</code> — это нужно для вложенных элементов вроде заливки ProgressBar.</p>',
  },
  'ui.facts': {
    en: facts('en', 'UI work can proceed separately from gameplay; the model is testable without Play mode; MVVM removes refresh boilerplate.', 'Architecture to plan up front; MVP needs glue code for every field; MVVM needs binding setup, converters and awareness of change detection cost.', 'Larger, long-lived UIs and teams. A single label on a prototype does not need either.'),
    ru: facts('ru', 'UI можно разрабатывать отдельно от геймплея; модель тестируется без Play mode; MVVM убирает шаблонный код обновления.', 'Архитектуру нужно продумать заранее; MVP требует связующий код для каждого поля; MVVM — настройку привязок, конвертеры и понимание цены отслеживания изменений.', 'Большие, долгоживущие интерфейсы и команды. Одной надписи в прототипе не нужно ни то, ни другое.'),
  },
  'ui.labTag': { en: 'INTERACTIVE · HEALTH BAR', ru: 'ИНТЕРАКТИВ · ПОЛОСКА ЗДОРОВЬЯ' },
  'ui.labH': { en: 'Follow one change through the layers — then rename the unit', ru: 'Проследи одно изменение через слои — затем переименуй юнита' },
  'ui.damage': { en: 'Click target (−15)', ru: 'Удар по цели (−15)' },
  'ui.restore': { en: 'Reset button', ru: 'Кнопка Reset' },
  'ui.rename': { en: 'Change LabelName in the asset', ru: 'Поменять LabelName в ассете' },
  'ui.stale': {
    en: '<b>Stale label.</b> In the book’s presenter <code>LabelName</code> is written once in <code>OnEnable</code>; renaming it raises no event, so the View shows the old name until something re-runs the setup.',
    ru: '<b>Устаревшая надпись.</b> В презентере из книги <code>LabelName</code> записывается один раз в <code>OnEnable</code>; переименование не поднимает событие, и View показывает старое имя, пока настройку не запустят снова.',
  },
  'ui.status.good': { en: 'Good', ru: 'Good' },
  'ui.status.neutral': { en: 'Neutral', ru: 'Neutral' },
  'ui.status.danger': { en: 'Danger', ru: 'Danger' },
  'ui.idle': { en: 'Press a button to trace the update.', ru: 'Нажми кнопку, чтобы проследить обновление.' },
  'ui.mvp.applyDamage': { en: 'View → <code>HealthPresenter.ApplyDamage(15)</code>', ru: 'View → <code>HealthPresenter.ApplyDamage(15)</code>' },
  'ui.mvp.restore': { en: 'View button → <code>HealthPresenter.RestoreHealth()</code>', ru: 'Кнопка View → <code>HealthPresenter.RestoreHealth()</code>' },
  'ui.mvp.modelChange': { en: 'Model: <code>CurrentHealth</code> changes', ru: 'Model: меняется <code>CurrentHealth</code>' },
  'ui.mvp.event': { en: 'Model raises <code>HealthChanged</code>', ru: 'Model поднимает <code>HealthChanged</code>' },
  'ui.mvp.handler': { en: 'Presenter: <code>OnHealthChanged()</code> → <code>UpdateUI()</code>', ru: 'Presenter: <code>OnHealthChanged()</code> → <code>UpdateUI()</code>' },
  'ui.mvp.updateUi': { en: 'Presenter writes bar value, bar colour, status and value label by hand', ru: 'Presenter вручную пишет значение полосы, цвет, статус и надпись' },
  'ui.mvp.renameModel': { en: 'Model: <code>LabelName</code> changes', ru: 'Model: меняется <code>LabelName</code>' },
  'ui.mvp.noEvent': { en: 'No event for this field → the View is not refreshed', ru: 'У этого поля нет события → View не обновляется' },
  'ui.mvvm.applyDamage': { en: 'View Model → <code>HealthModel.Decrement(15)</code>', ru: 'View Model → <code>HealthModel.Decrement(15)</code>' },
  'ui.mvvm.restore': { en: 'Button → <code>HealthModel.Restore()</code>', ru: 'Кнопка → <code>HealthModel.Restore()</code>' },
  'ui.mvvm.modelChange': { en: 'Model: <code>CurrentHealth</code> changes', ru: 'Model: меняется <code>CurrentHealth</code>' },
  'ui.mvvm.binding': { en: 'Binding system notices the source changed (mode <code>ToTarget</code>)', ru: 'Система привязок замечает изменение источника (режим <code>ToTarget</code>)' },
  'ui.mvvm.converter': { en: 'ConverterGroup “Int to HealthBar” → colour and “Good/Neutral/Danger”', ru: 'ConverterGroup «Int to HealthBar» → цвет и «Good/Neutral/Danger»' },
  'ui.mvvm.targets': { en: '<code>value</code>, <code>text</code>, <code>style.backgroundColor</code> updated — no UpdateUI', ru: 'Обновлены <code>value</code>, <code>text</code>, <code>style.backgroundColor</code> — без UpdateUI' },
  'ui.mvvm.renameModel': { en: 'Model: <code>LabelName</code> changes', ru: 'Model: меняется <code>LabelName</code>' },
  'ui.mvvm.bindingLabel': { en: 'Label is bound to <code>LabelName</code> → updated like any other property', ru: 'Надпись привязана к <code>LabelName</code> → обновляется как любое свойство' },
  'ui.compareRows': {
    en: [
      ['Who notices a change', 'Presenter subscribes to HealthChanged', 'Binding system checks bound sources'],
      ['Who formats data', 'UpdateUI() in the Presenter', 'Converters registered on the model or binding'],
      ['Glue code per field', 'A line in UpdateUI plus an event', 'A binding (UI Builder or SetBinding)'],
      ['Easy to debug', 'Yes — plain calls you can step through', 'Harder — updates happen inside the binding system'],
      ['Available', 'Any UI (UGUI or UI Toolkit)', 'UI Toolkit runtime binding, Unity 6'],
    ],
    ru: [
      ['Кто замечает изменение', 'Presenter подписан на HealthChanged', 'Система привязок проверяет источники'],
      ['Кто форматирует данные', 'UpdateUI() в Presenter', 'Конвертеры на модели или привязке'],
      ['Связующий код на поле', 'Строка в UpdateUI и событие', 'Привязка (UI Builder или SetBinding)'],
      ['Отладка', 'Проще — обычные вызовы, можно пройти по шагам', 'Сложнее — обновления внутри системы привязок'],
      ['Где доступно', 'Любой UI (UGUI или UI Toolkit)', 'Runtime binding в UI Toolkit, Unity 6'],
    ],
  },

  'fly.h2': { en: 'Flyweight: share what is the same, keep what differs', ru: 'Flyweight: общее — делить, различное — хранить' },
  'fly.prose': {
    en: '<p>A strategy game has a thousand units of the same faction. Each copies the faction name, icon, base stats — and every copy costs memory and must be kept in sync. The <b>flyweight</b> separates <i>intrinsic</i> data, identical for all (faction stats, a sway curve, LOD distances), from <i>extrinsic</i> data unique to each instance (health, position, target).</p><p>In the book, intrinsic data goes into a <code>ScriptableObject</code> — <code>FactionData</code> or <code>ShipData</code> — and each <code>Ship</code> keeps a reference to it plus its own health. A <code>ShipFactory</code> spawns the fleet and passes the shared asset to every ship. Check the savings in the Memory Profiler.</p><p>Prefabs are a flyweight of whole GameObject structures; a flyweight shares selected fields. For tens of thousands of objects the book points to DOTS, where data locality matters more than the per-object overhead a MonoBehaviour already carries.</p>',
    ru: '<p>В стратегии тысяча юнитов одной фракции. Каждый копирует имя фракции, иконку, базовые характеристики — каждая копия стоит памяти и требует синхронизации. <b>Flyweight</b> отделяет <i>внутренние</i> данные, одинаковые для всех (характеристики фракции, кривая покачивания, дистанции LOD), от <i>внешних</i>, уникальных для экземпляра (здоровье, позиция, цель).</p><p>В книге внутренние данные лежат в <code>ScriptableObject</code> — <code>FactionData</code> или <code>ShipData</code>, — а каждый <code>Ship</code> хранит ссылку на ассет и своё здоровье. <code>ShipFactory</code> создаёт флот и передаёт общий ассет каждому кораблю. Экономию проверяют в Memory Profiler.</p><p>Prefab — это flyweight для целой структуры GameObject; flyweight делится выбранными полями. Для десятков тысяч объектов книга отправляет в DOTS, где важнее расположение данных, чем накладные расходы, которые MonoBehaviour уже несёт.</p>',
  },
  'fly.facts': {
    en: facts('en', 'Less duplicated memory and one source of truth: changing the asset changes every unit.', 'Shared data must be treated as read-only at runtime; per-unit overrides need a separate layer.', 'Many instances share a meaningful amount of value data or serialized collections.'),
    ru: facts('ru', 'Меньше дублированной памяти и один источник правды: правка ассета меняет всех юнитов.', 'Общие данные во время игры надо считать только для чтения; индивидуальные отличия требуют отдельного слоя.', 'Много экземпляров делят заметный объём значимых данных или сериализованных коллекций.'),
  },
  'fly.labTag': { en: 'INTERACTIVE · UNIT MEMORY', ru: 'ИНТЕРАКТИВ · ПАМЯТЬ ЮНИТОВ' },
  'fly.labH': { en: 'Move fields into FactionData and see where the bytes actually come from', ru: 'Перенеси поля в FactionData и посмотри, откуда на самом деле берутся байты' },
  'fly.units': { en: 'Units of one faction', ru: 'Юнитов одной фракции' },
  'fly.fieldsHint': { en: 'Pressed = stored once in the shared asset. Health and position always stay per unit.', ru: 'Нажато — хранится один раз в общем ассете. Здоровье и позиция всегда остаются у юнита.' },
  'fly.perUnit': { en: 'PER UNIT', ru: 'У ЮНИТА' },
  'fly.always': { en: 'EXTRINSIC', ru: 'ВНЕШНЕЕ' },
  'fly.barCopy': { en: per => `Every unit copies everything · ${per} each`, ru: per => `Каждый юнит копирует всё · ${per} на юнита` },
  'fly.barFly': { en: (per, shared) => `Flyweight · ${per} each + ${shared} shared once`, ru: (per, shared) => `Flyweight · ${per} на юнита + ${shared} общих один раз` },
  'fly.thField': { en: 'Shared field', ru: 'Общее поле' },
  'fly.thStorage': { en: 'What a copy costs', ru: 'Цена копии' },
  'fly.thSaved': { en: 'Saved', ru: 'Экономия' },
  'fly.storage.value': { en: 'value bytes copied', ru: 'копия значимых байтов' },
  'fly.storage.reference': { en: 'only an 8-byte reference', ru: 'только ссылка 8 байт' },
  'fly.storage.serialized': { en: 'serialized collection duplicated by Unity', ru: 'сериализованная коллекция, Unity дублирует' },
  'fly.nothingShared': { en: 'Nothing shared yet.', ru: 'Пока ничего не общее.' },
  'fly.edit': { en: 'Designer changes baseAttack', ru: 'Дизайнер меняет baseAttack' },
  'fly.editOut': {
    en: r => r.writes === 1 ? `One write to the asset → <b>${r.unitsUpdated}</b> units see it immediately.` : `The value was copied into every unit: <b>${r.unitsStale}</b> units are stale until a sync pass writes ${r.writes} times.`,
    ru: r => r.writes === 1 ? `Одна запись в ассет → <b>${r.unitsUpdated}</b> юнитов видят её сразу.` : `Значение скопировано в каждого юнита: <b>${r.unitsStale}</b> юнитов устарели, пока проход синхронизации не сделает ${r.writes} записей.`,
  },
  'fly.errata': {
    en: '<b>Checked against the book</b><p>The book lists “a struct, primitive type, or array” as value types copied per GameObject. In C# an array is a reference type; it gets duplicated because Unity serialization stores and deserializes a separate array for every serialized instance. The same goes for <code>List&lt;T&gt;</code> and <code>AnimationCurve</code>. Strings and <code>Sprite</code> fields are references: moving them into a ScriptableObject saves a pointer each, not the string or the texture. Also do not modify a shared ScriptableObject at runtime in the Editor — the change is saved to the asset.</p>',
    ru: '<b>Сверено с книгой</b><p>Книга относит «struct, примитив или массив» к значимым типам, которые копируются в каждый GameObject. В C# массив — ссылочный тип; дублируется он потому, что сериализация Unity хранит и десериализует отдельный массив для каждого экземпляра. То же с <code>List&lt;T&gt;</code> и <code>AnimationCurve</code>. Строки и поля <code>Sprite</code> — ссылки: перенос в ScriptableObject экономит по указателю, а не строку или текстуру. И не меняй общий ScriptableObject во время игры в редакторе — изменение сохранится в ассет.</p>',
  },

  'dirty.h2': { en: 'Dirty Flag: remember that something changed, do the work later', ru: 'Dirty Flag: запомни, что изменилось, работу сделай потом' },
  'dirty.prose': {
    en: '<p>A <b>dirty flag</b> is a boolean that says “my state changed since it was last processed”. Expensive work runs only when the flag is set, then clears it. The pattern trades a cheap check for skipping repeated expensive work.</p><p>The book’s example streams an open world. The level is split into sectors; every frame <code>GameSectors</code> checks each sector’s distance to the player. The check is cheap and still runs every frame. Only when it disagrees with <code>IsLoaded</code> does the sector get <code>MarkDirty()</code>, and only a dirty sector calls the slow <code>LoadContent</code>/<code>UnloadContent</code> (additive scene loading) and then <code>Clean()</code>.</p><p>More uses: hierarchical transforms that recompute a world matrix only when a parent moved, path recalculation when obstacles change, procedural regeneration, UI layout. Unity has it too: <code>VisualElement.MarkDirtyRepaint()</code> and <code>EditorUtility.SetDirty()</code>. A <i>dirty bit</i> is the low-level cousin that marks modified memory pages; <i>caching</i> is the broader technique that often relies on dirty flags.</p>',
    ru: '<p><b>Dirty flag</b> — булево значение «моё состояние изменилось с последней обработки». Дорогая работа выполняется, только если флаг поднят, после чего флаг сбрасывается. Паттерн меняет дешёвую проверку на пропуск повторной дорогой работы.</p><p>Пример из книги — стриминг открытого мира. Уровень поделён на секторы; каждый кадр <code>GameSectors</code> проверяет расстояние от каждого сектора до игрока. Проверка дешёвая и всё равно идёт каждый кадр. Только когда её результат расходится с <code>IsLoaded</code>, сектор получает <code>MarkDirty()</code>, и только грязный сектор вызывает медленные <code>LoadContent</code>/<code>UnloadContent</code> (аддитивная загрузка сцен) и затем <code>Clean()</code>.</p><p>Другие применения: иерархические трансформы, которые пересчитывают мировую матрицу, только если сдвинулся родитель, пересчёт пути при изменении препятствий, процедурная генерация, раскладка UI. В Unity это тоже есть: <code>VisualElement.MarkDirtyRepaint()</code> и <code>EditorUtility.SetDirty()</code>. <i>Dirty bit</i> — низкоуровневый родственник, помечающий изменённые страницы памяти; <i>кэширование</i> — более широкая техника, часто опирающаяся на dirty flag.</p>',
  },
  'dirty.facts': {
    en: facts('en', 'Expensive work runs once per real change instead of every frame or every write.', 'Every mutation must set the flag or data goes stale; results lag until the next processing point; the flag couples writers to the cache.', 'The work is expensive, the input changes rarely compared to how often it is checked or read.'),
    ru: facts('ru', 'Дорогая работа выполняется один раз на реальное изменение, а не каждый кадр или при каждой записи.', 'Каждое изменение обязано поднять флаг, иначе данные устареют; результат отстаёт до следующей обработки; флаг связывает пишущий код с кэшем.', 'Работа дорогая, а вход меняется редко по сравнению с тем, как часто его проверяют или читают.'),
  },
  'dirty.labTag': { en: 'INTERACTIVE · SECTOR STREAMING', ru: 'ИНТЕРАКТИВ · СТРИМИНГ СЕКТОРОВ' },
  'dirty.labH': { en: 'The player walks; count the scene loads with and without the flag', ru: 'Игрок идёт; считаем загрузки сцен с флагом и без' },
  'dirty.pause': { en: '❚❚ Pause', ru: '❚❚ Пауза' },
  'dirty.play': { en: '► Play', ru: '► Пуск' },
  'dirty.radius': { en: 'Load radius', ru: 'Радиус загрузки' },
  'dirty.reset': { en: 'Reset counters', ru: 'Сбросить счётчики' },
  'dirty.canvasAria': { en: 'Top-down world split into nine sectors with a moving player', ru: 'Вид сверху на мир из девяти секторов с движущимся игроком' },
  'dirty.legend': {
    en: 'Green — loaded. Yellow flash — the frame where the flag was set and Load/Unload actually ran. Both versions end with exactly the same loaded sectors.',
    ru: 'Зелёный — загружен. Жёлтая вспышка — кадр, где флаг поднялся и Load/Unload действительно выполнились. Обе версии приходят к одинаковому набору загруженных секторов.',
  },
  'dirty.barChecks': { en: 'Distance checks (cheap, both versions)', ru: 'Проверки расстояния (дёшево, в обеих версиях)' },
  'dirty.barNaive': { en: 'Load/Unload calls without the flag', ru: 'Вызовы Load/Unload без флага' },
  'dirty.barDirty': { en: 'Load/Unload calls with the dirty flag', ru: 'Вызовы Load/Unload с dirty flag' },

  'lazy.labTag': { en: 'INTERACTIVE · LAZY RECALCULATION', ru: 'ИНТЕРАКТИВ · ЛЕНИВЫЙ ПЕРЕСЧЁТ' },
  'lazy.labH': { en: 'The classic form: set dirty on write, recompute on read', ru: 'Классическая форма: пометить при записи, пересчитать при чтении' },
  'lazy.share': { en: 'Writes among 120 operations', ru: 'Доля записей среди 120 операций' },
  'lazy.eager': { en: 'Eager recalcs', ru: 'Пересчётов сразу' },
  'lazy.eagerD': { en: 'one per write', ru: 'по одному на запись' },
  'lazy.lazy': { en: 'Dirty-flag recalcs', ru: 'Пересчётов с флагом' },
  'lazy.lazyD': { en: reads => `first read after a write · ${reads} reads`, ru: reads => `первое чтение после записи · чтений ${reads}` },
  'lazy.saved': { en: 'Skipped', ru: 'Пропущено' },
  'lazy.savedD': { en: 'writes nobody read', ru: 'записей, которые никто не прочитал' },
  'lazy.gotcha': {
    en: '<b>Interview trap</b><p>Pink cells are writes, bright green reads that recompute, dark green reads served from cache. With a hierarchy the flag must propagate: when a parent changes, every child’s cached world matrix is dirty too. Forgetting that is the classic stale-transform bug. Unity’s own <code>Transform</code> handles it internally; <code>transform.hasChanged</code> is a user-visible flag you must reset yourself.</p>',
    ru: '<b>Ловушка на собеседовании</b><p>Розовые клетки — записи, ярко-зелёные — чтения с пересчётом, тёмно-зелёные — чтения из кэша. В иерархии флаг должен распространяться: если изменился родитель, кэш мировой матрицы у всех детей тоже грязный. Забыть об этом — классический баг устаревшего трансформа. <code>Transform</code> в Unity делает это внутри; <code>transform.hasChanged</code> — видимый флаг, который сбрасывать нужно самому.</p>',
  },
};
