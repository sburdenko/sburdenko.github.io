/** Tape 05 strings: Command, State, Observer, Strategy. */
import { facts } from './facts.js?v=202609241230';

export const BEHAVIOR = {
  'command.h2': { en: 'Command: turn a call into an object', ru: 'Command: преврати вызов в объект' },
  'command.prose': {
    en: '<p>Instead of calling <code>PlayerMover.Move()</code> directly, the input code creates a <code>MoveCommand</code> that remembers the mover and the vector, and hands it to a <b>CommandInvoker</b>. The invoker runs <code>Execute()</code> and pushes the command onto an undo stack. <code>Undo()</code> pops it and applies the inverse — here, moving by <code>-movement</code>.</p><p>Because actions are now data, you can delay them, queue them, replay them or undo them: a turn-based planner, fighting-game combos, a replay system. Validation stays outside the command: <code>IsValidMove</code> raycasts for walls <i>before</i> a command is created, so the stack never holds an action that did not happen.</p><p>The book’s improvements are what you implement in an interview: a second stack for <b>redo</b> that is cleared by any new command, a size limit on the history, and a list with a cursor instead of two stacks if you need to scrub.</p>',
    ru: '<p>Вместо прямого вызова <code>PlayerMover.Move()</code> код ввода создаёт <code>MoveCommand</code>, который помнит исполнителя и вектор, и отдаёт его <b>CommandInvoker</b>. Инвокер вызывает <code>Execute()</code> и кладёт команду в стек отмены. <code>Undo()</code> достаёт её и применяет обратное действие — здесь это сдвиг на <code>-movement</code>.</p><p>Раз действия стали данными, их можно отложить, поставить в очередь, проиграть заново или отменить: пошаговый планировщик, комбо в файтинге, система повторов. Проверка остаётся снаружи команды: <code>IsValidMove</code> делает raycast к стенам <i>до</i> создания команды, поэтому в стеке не бывает действия, которого не было.</p><p>Улучшения из книги — то, что просят реализовать на собеседовании: второй стек для <b>redo</b>, который очищается любой новой командой, лимит длины истории и список с курсором вместо двух стеков, если нужна перемотка.</p>',
  },
  'command.facts': {
    en: facts('en', 'Undo/redo, replays, queued or delayed actions, input rebinding; the invoker knows nothing about what a command does.', 'A class per action, history memory, and Undo must restore everything Execute changed.', 'Actions must be stored, reversed, sent over the network or replayed.'),
    ru: facts('ru', 'Undo/redo, повторы, очереди и отложенные действия, переназначение ввода; инвокер не знает, что делает команда.', 'Класс на действие, память под историю, и Undo обязан вернуть всё, что изменил Execute.', 'Действия нужно хранить, отменять, передавать по сети или проигрывать заново.'),
  },
  'command.labTag': { en: 'INTERACTIVE · UNDOABLE MOVEMENT', ru: 'ИНТЕРАКТИВ · ОТМЕНЯЕМОЕ ДВИЖЕНИЕ' },
  'command.labH': { en: 'Walk the maze, undo, then make a new move and watch redo disappear', ru: 'Пройди лабиринт, отмени ход, затем сделай новый — и redo исчезнет' },
  'command.padAria': { en: 'Movement buttons', ru: 'Кнопки движения' },
  'command.mazeAria': { en: 'Maze with the player and the replay path', ru: 'Лабиринт с игроком и путём повтора' },
  'command.undo': { en: '↶ Undo', ru: '↶ Отменить' },
  'command.redo': { en: '↷ Redo', ru: '↷ Повторить' },
  'command.replay': { en: '► Replay history', ru: '► Проиграть историю' },
  'command.reset': { en: 'Reset', ru: 'Сброс' },
  'command.limit': { en: 'History limit', ru: 'Лимит истории' },
  'command.hintStart': { en: 'Start in the top-left corner. Walls block the raycast.', ru: 'Старт в левом верхнем углу. Стены блокируют raycast.' },
  'command.hintMove': { en: '<code>new MoveCommand(...)</code> → <code>Execute()</code> → pushed to undo; redo cleared.', ru: '<code>new MoveCommand(...)</code> → <code>Execute()</code> → в стек undo; redo очищен.' },
  'command.hintBlocked': { en: '<code>IsValidMove</code> returned false — no command was created, the history is unchanged.', ru: '<code>IsValidMove</code> вернул false — команда не создана, история не изменилась.' },
  'command.hintDropped': { en: n => `History is full: the oldest command was dropped (${n} total). Replay now starts from a later position.`, ru: n => `История полна: самая старая команда выброшена (всего ${n}). Повтор теперь начинается с более поздней позиции.` },
  'command.hintUndo': { en: 'Popped from undo, <code>Undo()</code> moved by −movement, pushed to redo.', ru: 'Снята с undo, <code>Undo()</code> сдвинул на −movement, положена в redo.' },
  'command.hintRedo': { en: 'Popped from redo, <code>Execute()</code> again, pushed back to undo.', ru: 'Снята с redo, снова <code>Execute()</code>, возвращена в undo.' },
  'command.hintReplay': { en: 'Replaying the stored commands from the oldest one.', ru: 'Проигрываем сохранённые команды, начиная с самой старой.' },

  'state.h2': { en: 'State: each mode is an object that decides when to leave', ru: 'State: каждый режим — объект, который сам решает, когда уйти' },
  'state.prose': {
    en: '<p>A character idles, walks and jumps; only one of these is active. The naive version is an enum and a <code>switch</code> in <code>Update</code>: every new state reopens the same class, and the conditions for switching are scattered across cases.</p><p>The state pattern gives each state its own class implementing <code>IState</code> with <code>Enter()</code>, <code>Execute()</code> (runs every frame) and <code>Exit()</code>. A small serializable <code>StateMachine</code> holds <code>CurrentState</code> and does one thing in <code>TransitionTo</code>: exit the old state, switch, enter the new one. The <b>states themselves</b> check conditions — <code>IdleState</code> watches velocity and grounding and asks the machine to move to walk or jump.</p><p>Extensions from the book: raise events on enter/exit for UI and audio, build hierarchical machines (a <code>GroundedState</code> super-state shared by walk and run), use it for simple AI (patrol, chase, attack) and for game flow. Unity’s Animator Controller is the same idea for animation.</p>',
    ru: '<p>Персонаж стоит, идёт и прыгает; активно только одно. Наивная версия — enum и <code>switch</code> в <code>Update</code>: каждое новое состояние снова открывает тот же класс, а условия перехода размазаны по веткам case.</p><p>Паттерн State даёт каждому состоянию свой класс с <code>IState</code>: <code>Enter()</code>, <code>Execute()</code> (каждый кадр) и <code>Exit()</code>. Маленький сериализуемый <code>StateMachine</code> хранит <code>CurrentState</code> и в <code>TransitionTo</code> делает одно: выйти из старого, переключиться, войти в новое. Условия проверяют <b>сами состояния</b> — <code>IdleState</code> смотрит на скорость и контакт с землёй и просит машину перейти в walk или jump.</p><p>Расширения из книги: события на вход и выход для UI и звука, иерархические машины (суперсостояние <code>GroundedState</code> для walk и run), простой ИИ (патруль, погоня, атака) и поток игры. Animator Controller в Unity — та же идея для анимации.</p>',
  },
  'state.facts': {
    en: facts('en', 'A new state does not touch existing ones; enter/exit logic runs exactly once; illegal combinations like “jumping while idle” cannot exist.', 'More classes and a place to share data between states. Overkill for two or three trivial modes.', 'The object has exclusive modes with their own behaviour and the switch keeps growing.'),
    ru: facts('ru', 'Новое состояние не трогает существующие; логика входа и выхода выполняется ровно один раз; недопустимых сочетаний вроде «прыгает и стоит» не бывает.', 'Больше классов и место для общих данных между состояниями. Избыточно для двух-трёх простых режимов.', 'У объекта взаимоисключающие режимы со своим поведением, и switch продолжает расти.'),
  },
  'state.labTag': { en: 'INTERACTIVE · PLAYER STATE MACHINE', ru: 'ИНТЕРАКТИВ · МАШИНА СОСТОЯНИЙ ИГРОКА' },
  'state.labH': { en: 'Move and jump: the active state decides every transition', ru: 'Двигайся и прыгай: переходы решает активное состояние' },
  'state.left': { en: '◀ Hold left', ru: '◀ Держать влево' },
  'state.right': { en: 'Hold right ▶', ru: 'Держать вправо ▶' },
  'state.jump': { en: 'Jump', ru: 'Прыжок' },
  'state.keys': { en: 'or A/D · ←/→ · Space while the rig is on screen', ru: 'или A/D · ←/→ · Пробел, пока стенд на экране' },
  'state.canvasAria': { en: 'Capsule character coloured by its current state', ru: 'Персонаж-капсула, окрашенный по текущему состоянию' },
  'state.fsmAria': { en: 'State diagram with idle, walk and jump', ru: 'Диаграмма состояний idle, walk и jump' },
  'state.logEmpty': { en: 'No transitions yet. Only Execute() runs each frame.', ru: 'Переходов пока нет. Каждый кадр выполняется только Execute().' },

  'observer.h2': { en: 'Observer: one event, any number of listeners', ru: 'Observer: одно событие, сколько угодно слушателей' },
  'observer.prose': {
    en: '<p>When an enemy dies, the score, the achievements, the audio and the UI all want to know. If the enemy calls each of them, it depends on all of them. The <b>observer</b> pattern flips this: the <i>subject</i> announces that something happened, and <i>observers</i> subscribe. The subject does not know who listens.</p><p>C# has it built in. The subject declares <code>public event Action ThingHappened</code> and raises it with <code>ThingHappened?.Invoke()</code>; observers add a handler with <code>+=</code> and remove it with <code>-=</code>. Use <code>Action&lt;T&gt;</code> to pass data. Naming from the book: the event is a past-tense fact (<code>DoorOpened</code>), the handler starts with <i>On</i>, the raising method is a verb.</p><p><b>UnityEvent</b> is the Inspector-friendly version — designers wire responses without code, as with a UI Button’s OnClick — but it is slower than C# events. The book’s improvements: <code>ObservableCollection</code>, passing an instance ID so only the matching object reacts, a static <code>EventManager</code> or ScriptableObject event channels, and an event queue combined with Command.</p>',
    ru: '<p>Когда умирает враг, об этом хотят знать счёт, достижения, звук и UI. Если враг вызывает каждого, он зависит от всех. Паттерн <b>Observer</b> переворачивает связь: <i>субъект</i> сообщает, что что-то случилось, а <i>наблюдатели</i> подписываются. Субъект не знает, кто слушает.</p><p>В C# он встроен. Субъект объявляет <code>public event Action ThingHappened</code> и поднимает его через <code>ThingHappened?.Invoke()</code>; наблюдатели добавляют обработчик через <code>+=</code> и убирают через <code>-=</code>. Для данных — <code>Action&lt;T&gt;</code>. Имена по книге: событие — свершившийся факт (<code>DoorOpened</code>), обработчик начинается с <i>On</i>, метод, поднимающий событие, — глагол.</p><p><b>UnityEvent</b> — версия для Inspector: дизайнер связывает реакции без кода, как в OnClick у кнопки UI, но работает медленнее событий C#. Улучшения из книги: <code>ObservableCollection</code>, передача instance ID, чтобы реагировал только нужный объект, статический <code>EventManager</code> или каналы событий на ScriptableObject и очередь событий вместе с Command.</p>',
  },
  'observer.facts': {
    en: facts('en', 'The subject does not depend on its listeners; new reactions are added without editing gameplay code; natural fit for UI.', 'Indirect control flow that is harder to trace, subscription lifetime bugs, and a small invocation cost.', 'Several independent systems react to the same fact and the subject should not know them.'),
    ru: facts('ru', 'Субъект не зависит от слушателей; новые реакции добавляются без правки игрового кода; естественно ложится на UI.', 'Непрямой поток управления, который сложнее проследить, баги времени жизни подписок и небольшая цена вызова.', 'Несколько независимых систем реагируют на один факт, и субъект не должен их знать.'),
  },
  'observer.labTag': { en: 'INTERACTIVE · SUBSCRIPTIONS AND LIFETIME', ru: 'ИНТЕРАКТИВ · ПОДПИСКИ И ВРЕМЯ ЖИЗНИ' },
  'observer.labH': { en: 'Raise the event, then destroy a listener the wrong way', ru: 'Подними событие, затем уничтожь слушателя неправильно' },
  'observer.reset': { en: 'Reset', ru: 'Сброс' },
  'observer.on': { en: 'subscribed', ru: 'подписан' },
  'observer.off': { en: 'disabled', ru: 'выключен' },
  'observer.dead': { en: 'destroyed', ru: 'уничтожен' },
  'observer.count': { en: n => `${n} handled`, ru: n => `обработано: ${n}` },
  'observer.destroy': { en: 'Destroy (OnDisable runs)', ru: 'Destroy (OnDisable вызван)' },
  'observer.leak': { en: 'Destroy, forgot −=', ru: 'Destroy, забыли −=' },
  'observer.logDestroy': { en: name => `${name}: OnDisable → ThingHappened -= OnThingHappened → Destroy`, ru: name => `${name}: OnDisable → ThingHappened -= OnThingHappened → Destroy` },
  'observer.logLeak': { en: name => `${name}: destroyed, but its handler is still in the delegate list`, ru: name => `${name}: уничтожен, но его обработчик остался в списке делегата` },
  'observer.log.handled': { en: name => `${name}.OnThingHappened()`, ru: name => `${name}.OnThingHappened()` },
  'observer.log.missing': {
    en: name => `${name}.OnThingHappened() on a destroyed object → MissingReferenceException when it touches gameObject; the delegate also keeps it from being garbage-collected`,
    ru: name => `${name}.OnThingHappened() у уничтоженного объекта → MissingReferenceException при обращении к gameObject; делегат ещё и не даёт собрать его сборщиком мусора`,
  },
  'observer.logNobody': { en: 'Invoke: no subscribers, nothing happens', ru: 'Invoke: подписчиков нет, ничего не происходит' },
  'observer.errata': {
    en: '<b>Checked against the book</b><p>The text says to unsubscribe in <code>OnDestroy</code>, while the code subscribes in <code>OnEnable</code> and unsubscribes in <code>OnDisable</code>. Pick one symmetric pair: <code>OnEnable/OnDisable</code> (stops listening while disabled) or <code>Awake/OnDestroy</code> (listens while disabled). Subscribing in <code>Start</code> and unsubscribing in <code>OnDisable</code> loses the subscription after re-enabling. Static events are the riskiest: the static delegate keeps every forgotten listener alive until the domain reloads.</p>',
    ru: '<b>Сверено с книгой</b><p>В тексте сказано отписываться в <code>OnDestroy</code>, а в коде подписка в <code>OnEnable</code> и отписка в <code>OnDisable</code>. Выбирай одну симметричную пару: <code>OnEnable/OnDisable</code> (не слушает, пока выключен) или <code>Awake/OnDestroy</code> (слушает и выключенным). Подписка в <code>Start</code> с отпиской в <code>OnDisable</code> теряется после повторного включения. Опаснее всего статические события: статический делегат держит каждого забытого слушателя до перезагрузки домена.</p>',
  },

  'strategy.h2': { en: 'Strategy: the same slot, a different algorithm', ru: 'Strategy: один слот, разный алгоритм' },
  'strategy.prose': {
    en: '<p>A stealth game switches between sneaking and running; combat switches between melee, ranged and magic. With an enum and a <code>switch</code>, every new behaviour edits the same method. The <b>strategy</b> pattern wraps each algorithm in its own object behind a shared contract, and the client just calls it.</p><p>In the book, <code>Ability</code> is an abstract <code>ScriptableObject</code> with <code>Use(GameObject)</code>. <code>RadarPulse</code>, <code>AirSupport</code> and <code>FirstAid</code> are assets; <code>AbilityRunner</code> holds a <code>currentAbility</code> reference and calls <code>Use</code> on Space. Because strategies are assets, a designer can swap them in the Inspector, and gameplay can swap them at runtime — the sample picks the perk from the player’s streak.</p><p>Other uses from the book: movement (walk → double jump → dash), AI modes, pathfinding algorithms (A*, Dijkstra), weapon types, adaptive difficulty. Strategy and State look alike in code; the difference is who switches: a strategy is chosen by the client, a state chooses its successor.</p>',
    ru: '<p>Стелс-игра переключается между подкрадыванием и бегом, бой — между ближним, дальним и магией. С enum и <code>switch</code> каждое новое поведение правит один и тот же метод. Паттерн <b>Strategy</b> заворачивает каждый алгоритм в отдельный объект за общим контрактом, а клиент просто его вызывает.</p><p>В книге <code>Ability</code> — абстрактный <code>ScriptableObject</code> с <code>Use(GameObject)</code>. <code>RadarPulse</code>, <code>AirSupport</code> и <code>FirstAid</code> — ассеты; <code>AbilityRunner</code> держит ссылку <code>currentAbility</code> и вызывает <code>Use</code> по пробелу. Раз стратегии — ассеты, дизайнер меняет их в Inspector, а игра — во время игры: в примере перк выбирается по серии игрока.</p><p>Другие применения из книги: движение (ходьба → двойной прыжок → рывок), режимы ИИ, алгоритмы поиска пути (A*, Дейкстра), типы оружия, адаптивная сложность. В коде Strategy и State похожи; разница в том, кто переключает: стратегию выбирает клиент, состояние само выбирает следующее.</p>',
  },
  'strategy.facts': {
    en: facts('en', 'Behaviours are swapped at runtime or in the Inspector; new ones are added without editing the runner; each is testable alone.', 'More classes and assets; strategies need a way to share data without coupling back to the client.', 'Several interchangeable algorithms for one job, selected by data or gameplay.'),
    ru: facts('ru', 'Поведение меняется во время игры или в Inspector; новое добавляется без правки раннера; каждое тестируется отдельно.', 'Больше классов и ассетов; стратегиям нужен способ делиться данными, не связываясь обратно с клиентом.', 'Несколько взаимозаменяемых алгоритмов для одной задачи, выбор — по данным или геймплею.'),
  },
  'strategy.labTag': { en: 'INTERACTIVE · STREAK PERKS', ru: 'ИНТЕРАКТИВ · ПЕРКИ ЗА СЕРИЮ' },
  'strategy.labH': { en: 'Build a streak, use the current perk, then add a new one', ru: 'Набери серию, используй текущий перк, потом добавь новый' },
  'strategy.pickup': { en: '+ Power-up (streak +1)', ru: '+ Усиление (серия +1)' },
  'strategy.hit': { en: 'Take a hit (streak 0)', ru: 'Получить урон (серия 0)' },
  'strategy.add': { en: '+ Add ShieldWall perk (streak 9)', ru: '+ Добавить перк ShieldWall (серия 9)' },
  'strategy.canvasAria': { en: 'Arena where the current ability plays its effect', ru: 'Арена, где текущая способность показывает эффект' },
  'strategy.use.radar': { en: 'RadarPulse.Use → hidden enemies revealed', ru: 'RadarPulse.Use → скрытые враги показаны' },
  'strategy.use.firstAid': { en: 'FirstAid.Use → squad healed', ru: 'FirstAid.Use → отряд вылечен' },
  'strategy.use.airSupport': { en: 'AirSupport.Use → strikes on every target', ru: 'AirSupport.Use → удары по всем целям' },
  'strategy.use.shield': { en: 'ShieldWall.Use → barrier raised', ru: 'ShieldWall.Use → поднят барьер' },
  'strategy.edited': { en: item => `edited existing code: ${item}`, ru: item => `правка существующего кода: ${item}` },
  'strategy.added': { en: item => `added: ${item}`, ru: item => `добавлено: ${item}` },
};
